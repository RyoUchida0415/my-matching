<%= render 'users/header' %>

<div class="main clearfix">
<div class="col-md-3">
	<%= render 'users/sidebar', user: current_user %>
</div>

<div class="col-md-9">
	<h2>フォロワー会員</h2>
	<!-- フォロワーが存在するならば -->
	<% if @users.count > 0 %>
	<%= render '/users/index', users: @users %>
	<!-- フォロワーが存在しないならば -->
	<% else %>
	<p>ユーザーはいません</p>
	<% end %>
</div>
</div>

<%= render 'users/footer' %>